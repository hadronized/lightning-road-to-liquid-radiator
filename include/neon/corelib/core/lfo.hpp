#ifndef NE__LFO_HPP
#define NE__LFO_HPP
/*
#include <core/controller.hpp>
#include <core/parametrable.hpp>

namespace Ne
{
	class LFO
		: public Parametrable,
		  public Controller
	{
	private:
		float        m_phase;
		unsigned int m_shape;

		float        m_output;
	public:
		enum Parameters
		{
			RATE = 0,
			RANGE_MIN,
			RANGE_MAX,

			N_PARAMETERS
		};

		enum ExposedValues
		{
			OUTPUT = 0,
			N_EXPOSED_VALUES
		};

		enum Shape
		{
			Sine = 0,
			Saw,
			Square
		};

		LFO(unsigned int shape);
		void process(float cTime);
	};
}
*/
#endif /* NE__LFO_HPP */

